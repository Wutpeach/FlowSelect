# Compact mascot ear-root integration implementation

## Implementation

- [ ] In `renderCompactMascotScene`, preserve all core-projected canonical ear
  paths/IDs while assigning both partitions to final Compact `backPaths` and
  leaving final `frontPaths` empty.
- [ ] Extend the existing behavior-runtime test seam for neutral, surprised,
  curious-short, playful-short, bounded pointer, and Reduced Motion invariants;
  compare output with the unchanged core-projected paths.
- [ ] Update the Compact character-motion contract wording for composition-owned
  final ear layering without changing other authority boundaries.
- [ ] Reuse the existing Lab evidence seam to capture task-local black/white 1x
  and 3x evidence for representative poses, bounded pointer, and Reduced Motion;
  assert back slots are non-empty and front slots are empty.
- [ ] Record native-holder visibility honestly; do not fabricate a screenshot.
- [ ] Write a task-local implementation report and stop without Architecture
  PASS, geometry refinement, OneWorks cleanup, or follow-on work.

## Validation

- [ ] Run focused Compact runtime, definition, surface, architecture/import
  guard, Lab stage/scenario, and renderer-reuse tests.
- [ ] Run `npm run type-check` and `npm run lint`.
- [ ] Run Trellis task validation and task-scoped whitespace/diff checks while
  preserving known unrelated CRLF noise.
- [ ] Review visual evidence for natural head/body root occlusion and visible
  exterior ears in neutral, retained actions, and bounded-pointer state.

## Risk and rollback points

- Preserve path/ID pairing while flattening partitions; tests must catch drops
  or reordering.
- Do not reset, clean, restore, or rewrite unrelated dirty files or 09-07
  historical evidence.
- If the Lab capture seam cannot run, report the exact limitation and retain
  automated DOM-layer evidence rather than inventing visual proof.
