# Agentation Integration Spike Execution Plan

## 1. Official Mount

1. Install exact `agentation@3.0.2` as a devDependency.
2. Mount one unconfigured official component from `src/lab/lab-main.tsx` only.
3. Add the smallest existing-test boundary assertions for Lab-only import and production-entry absence.

## 2. Real-Browser Proof

1. Run UI Lab in a normal desktop browser viewport and capture tightly cropped reviewable evidence.
2. Exercise the official toolbar, Full at 1x/Auto/3x, target/scale changes, area/draw annotation, shared DOM overlay/popover selection, Compact stage/shell, and Compact area annotation.
3. Exercise Reset, Background, queue/runtime controls, and annotation mode recovery.
4. Record exact target DOM/component/source output from copied annotation data.
5. Compare Full origin before and after annotation. Add only the allowed Lab guard if reproduced and sufficient.

## 3. Production Isolation

1. Run focused tests, type-check, lint, and production build.
2. Scan `dist/` and `dist-electron/` for Agentation identifiers and dedicated assets.
3. Build the current-platform unpacked production package.
4. Scan unpacked files and extracted/listed asar contents for Agentation code, identifiers, assets, or package content.

## 4. Report and Stop

1. Write task-local browser and isolation evidence plus the Spike Implementation Report.
2. Report GO or NO-GO and whether the exact dependency should remain.
3. Stop without Architecture PASS, archive, feature expansion, or Compact Mascot work.

## 5. Final Placement Repair

1. Group Background and Reset at the Preview lower-left, preserving local state, tooltip/accessibility, and reset behavior.
2. Pass only the official Agentation `className` prop and use Lab CSS to place its unchanged fixed toolbar near the Preview lower-right.
3. Re-run Full Auto/manual, Compact, menu, target/scale switching, annotation, origin-guard, and artifact-isolation proof.
4. If no new blocker appears, update the final report, commit the bounded Agentation changes, and archive the spike and parent planning task.
