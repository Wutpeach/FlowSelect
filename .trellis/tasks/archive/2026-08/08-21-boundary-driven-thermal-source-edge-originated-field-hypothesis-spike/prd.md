# Boundary-Driven Thermal Source / Edge-Originated Field Hypothesis Spike

## Goal

Validate, in a bounded Browser Lab spike, whether one lower-left to upper-right travelling source contour and its broad influence can cause edge-originated entry, developed full-surface Thermal presence, and edge-directed exit. The accepted 2D multi-temperature material and Thermal Refraction remain material responses; they do not own lifecycle disappearance.

## Background and Confirmed Facts

- The authoritative line is the clean `motion/mr9-fullscreen-activation-fx` worktree at `eaead65`.
- The accepted Thermal + Refraction + 2D multi-temperature work is committed at `046f228`; the stable checkpoint named by the rejected follow-up is `8a80474`.
- Directional Exit / Terminal Convergence was rejected and archived as an abandoned visual experiment. Its failed changes to `ExpandedPresentationSurface.tsx` and `expandedPresentationSurface.test.ts` were restored; `8a80474..HEAD` contains no production-code delta.
- The rejected task's research, eight captures, sequence sheet, capture log, report, and validation evidence remain archived.
- The existing shader already has the useful source-side vocabulary: one phase `k`, travelling coordinate `front`, direction `DIR`, bent signed contour distance `d`, low-frequency `heatmapBend`, real rounded-boundary SDF/band, and contact/frontier scalars.
- The accepted 2D temperature topology is already separated from coverage. It is built from three broad transformed `heatmapBend` samples and projected through the existing seven-stop palette.
- Current presence is still accumulated by half-plane coverage (`covered` / `covered2`) and removed by the phase-wide `energy` envelope. Those coverage/lifecycle signals are reopened by this task.
- The renderer remains one `ExpandedPresentationSurface`, one canvas, one WebGL2 program, one draw, and one renderer-local runtime. Reduced Motion pins `k = 0.42`, pins `bendTime = 0`, and schedules no continuing Heatmap frames.

## Requirements

### R1. Preserve the stable checkpoint and rejected evidence

- Do not revive, commit, or repair the rejected compression / containment / terminal convergence implementation.
- Keep the archived Directional Exit task and all captures/reports as failure evidence.
- Keep production code at the accepted Thermal + Refraction + 2D multi-temperature checkpoint before any future spike begins.

### R2. One source/influence causality

- Reinterpret the existing travelling contour (`front` plus bent signed distance `d`) as a spatial source, not as the edge of an accumulating material object. Reuse the signal roles, but reopen the current `front = mix(-0.58, 1.6, k)` mapping because it does not begin outside the source support.
- Derive one broad, smooth, finite-support influence from distance to that contour. The same influence must drive Thermal visibility and Refraction intensity.
- The source path must continue monotonically along the existing `DIR` from outside/near the lower-left boundary, through the window, and beyond the upper-right boundary.
- The influence support must be broad enough that a middle interval produces developed full-surface presence without creating an inset shape or closed blob.
- For every fixed point in the window, influence must become exactly zero after the source support has passed it. Do not use a phase-wide global opacity fade or terminal cleanup scalar as the primary or final exit mechanism.
- Keep Reduced Motion's existing authority and pinned `k = 0.42`, but map that pinned phase to a developed source position near the window's source-space center. Do not inherit the old `front(0.42) = 0.336` assumption blindly.

### R3. Preserve material and optical identity

- Keep the accepted seven-stop palette, broad 2D multi-temperature topology, material grain, and localized hot/cool relationships.
- Keep the existing low-frequency `heatmapBend` as both broad material topology input and Refraction direction source.
- Replace all old half-plane/phase visibility authority in the spike: `behind`, `body`, `energy`, `covered`, `covered2`, and `bodyFloor` must not leave material visible outside the source influence. The signed source coordinate may remain for contour/refraction math, but the one source influence answers where the accepted material and Refraction can respond.
- Keep rounded-boundary contact local: boundary contribution is the intersection of the real rounded-boundary band and the source contour/influence, never a perimeter path or outline.
- Define zero as zero activated Thermal/Refraction delta relative to the stable pre-entry void output. Before-entry and post-exit frames must be pixel-equivalent; the existing navy/alpha void floor may remain only if it is identical on both sides and is not a terminal fade.

### R4. Ownership and accessibility

- Keep Product, Download, lifecycle, Presentation policy, native window, and semantic targets unchanged.
- Keep the current renderer time and renderer-local `k` as the only spike motion authority; add no clock, scheduler, oscillator, scene, layer, field, choreography, or terminal state abstraction.
- Keep one surface, canvas, WebGL2 program, draw, and renderer-local runtime.
- Reduced Motion must remain a static developed snapshot with zero continuing frames.

### R5. Visual exclusions

- Do not introduce internal synthetic geometry, inset forms, logo/diamond fidelity, a moving blob/object, visible source dot, narrow source strip, perimeter outline/chase, opposite closure, wipe/deletion mask, shrinking material, compressed icon, soft containment ellipse, or independent terminal choreography.
- Paper Shaders may inform the high-level distinction between contour, inner/outer influence, and material response. Do not copy source or reopen literal Paper geometry/fidelity.

### R6. Bounded future spike evidence

- Keep the future implementation Browser Lab-only and uncommitted until GPT Architecture Lead review.
- Capture source-before-entry, first boundary influence, a mid-ramp frame that exposes the spatial falloff, developed early/middle/late, source-near-exit, post-boundary residual, and exact-zero states, plus Reduced Motion.
- Record WebGL resource evidence and compare the accepted material identity in developed frames.
- Stop immediately if the influence edge reads as a wipe, the source reads as a blob/object, developed coverage is not broad, or exit requires a separate cleanup choreography.

## Acceptance Criteria

- [ ] AC1: Entry begins as localized influence at the real lower-left rounded boundary and develops inward without an inset carrier or closed object.
- [ ] AC2: A middle interval shows broad/full-surface accepted 2D multi-temperature material with Refraction, not a narrow travelling strip.
- [ ] AC3: Exit is the same source support passing beyond the real upper-right boundary; each fixed pixel's activated influence reaches zero from source distance alone, and post-exit output is pixel-equivalent to pre-entry void output.
- [ ] AC4: No global alpha exit, diagonal fade/wipe, moving mask, visible source dot/strip, compression, containment ellipse, shrinking blob, perimeter grammar, or terminal choreography is present.
- [ ] AC5: Palette, 2D temperature topology, Refraction identity/strength hierarchy, real boundary, direction, and Reduced Motion authority remain recognizable and source-contract protected.
- [ ] AC6: One surface/canvas/program/draw/runtime and existing time authority remain; no new architecture primitive is introduced.
- [ ] AC7: The required phase sequence and resource evidence are captured, and direct visual review answers entry, developed presence, and exit separately.
- [ ] AC8: Focused tests, type-check, lint, full tests, and `git diff --check` are run with any pre-existing failure documented.

## Out of Scope

- Fast-slow-fast timing tuning.
- Production integration, lifecycle wiring, public documentation, packaging, release work, or a commit of the spike.
- Palette, 2D temperature topology, or Refraction identity redesign.
- Perimeter Chase, Opposite Closure, Directional Exit repair, containment/compression, Mask/Noise Dissolve, or terminal convergence.
- Generic field, scene, layer, post-processing, choreography, or material frameworks.
- Literal Paper fidelity, Paper logo/diamond geometry, or adapted Paper source.
