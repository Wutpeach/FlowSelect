# Later Browser Lab Spike Plan — Not Authorized In This Task

## Preconditions

- Preserve the rejected Unified Boundary evidence and the dirty worktree diff as frozen
  evidence. Before implementation, restore the worktree source to the accepted HEAD checkpoint
  (`4db7722`) so the spike starts from the accepted Thermal + Refraction baseline.
- Obtain GPT Architecture Lead Planning Approval for the repaired `design.md`.
- This task is planning-only; nothing below is authorized here.

## Minimum Change Surface (boundary response + halo ONLY)

No Entry/Exit, traversal, timing, or Refraction redesign. The spike consumes the existing
accepted `capture`/`contact` (= `boundaryBand * warm`) scalar and adds only an outward halo
projection of it.

1. `ExpandedPresentationSurface.tsx`
   - keep one program/draw, existing context-loss/dispose lifecycle, accepted interior and
     Refraction paths byte-equivalent;
   - add one Lab-only boundary/halo gate that consumes ONLY the existing
     `capture`/`contact` scalar;
   - project the accepted rounded SDF outward normal and distance as pure geometry;
   - sample the chosen interaction at the boundary foot; multiply by the analytic
     compact-support falloff (<=12 CSS px, exact zero in the final 2px);
   - same `HEAT_STOP[7]` palette with subordinate alpha;
   - expose actual renderer shader compile/link diagnostics to the Lab.
2. `expandedPresentationSurface.test.ts`
   - source/authority contracts: one canvas/program/draw, no texture/FBO/second scheduler,
     mode-off equivalence, boundary response derived only from `capture`/`contact`, no `E`/`H`/
     `baseTemperature` response, no traversal identifiers.
3. `LabOverlayStage.tsx`, `scenarios.ts`, `PresentationLab.tsx`, locales, focused Lab tests
   - reuse the validated 228 shell/canvas/interactive-clip split (real panel at (14,14)/228,
     FX canvas non-interactive, one CSS shadow owner);
   - add one live replay preset and deterministic evidence checkpoints for the accepted source
     signals, the chosen interaction, the halo, and the final composite;
   - show actual component compile/link/resource diagnostics.
4. Runtime source (`expandedPresentationRuntime.ts`) remains unchanged. Runtime tests still
   prove at most one pending frame and Reduced Motion zero continuing frames.

No Product policy, target shape, native window, dependency, texture, framebuffer, second pass,
or production caller change belongs in the spike.

## One-Round Evidence Sequence

- Live actual-component replay of the accepted activation age driving the localized boundary
  response and halo through rise and terminal zero.
- Native canvas PNG checkpoints: pre-contact, first boundary contact, localized lit arc,
  developed early, developed middle, decay, just-before-zero, exact zero, Reduced Motion.
- Debug PNGs from the same component/program: accepted source signals (`warm`, `boundaryBand`,
  `capture`/`contact`), chosen interaction, halo, and final composite.
- Four-side / four-corner outer alpha evidence: outermost 2px of the halo exactly zero on all
  sides and corners.
- FX off/on shell/shadow pair and hit-testing/drag evidence; one CSS shadow owner.
- Refraction off/on pair at the same phase (identity lock evidence).
- JSON: uniforms/phases, compile/link logs, canvas/program/draw/texture/FBO counters, boundary-
  lit fraction per phase, interior/edge/halo occupancy, four-side/four-corner alpha, Reduced
  Motion frame count, fallback hashes.
- Compact landscape review sheet (maximum 4 columns per row) plus raw alpha PNGs; no tall blank
  full-page evidence.

## Hard Validation Gates

- Actual `ExpandedPresentationSurface` fragment shader compiles and the real Lab program links
  (a copied harness cannot satisfy this gate).
- One canvas, one linked program, one draw per render; no texture, framebuffer, second renderer,
  or second scheduler.
- Lab mode off is pixel/resource equivalent to accepted HEAD.
- Boundary response derives only from the existing `capture`/`contact` scalar; no `E`, `H`,
  gradient, normal-derivative, or absolute-energy threshold enters.
- Boundary-lit fraction stays localized and visibly broken at every phase (target well below
  35%; any near-complete side/perimeter is failure; the rejected 0.979 S4 value is the
  reference failure).
- Halo support at most 12 CSS px; outermost 2px exactly alpha zero on four sides and corners.
- Interior and Refraction stay locked: mode-off pixel diff zero; Refraction identity unchanged
  when the halo gate is on.
- Reduced Motion is deterministic and schedules zero continuing frames.
- FX canvas remains `pointer-events:none`; UI descendants interact normally; one CSS shadow
  owner; transparent compositing valid.
- Windows transparent host verified; macOS explicitly `NOT VERIFIED` when no host exists.

## Immediate Falsification (stop on any, no parameter repair loop)

- Near-complete perimeter (boundary-lit fraction approaching a full outline) at any phase.
- Interaction detached from the existing contact/frontier scalar (response lit by any other
  source).
- Independent halo (own clock/topology/palette) or nonzero outer edge (final 2px not exactly
  zero).
- Refraction/interior drift (mode-off pixel diff, Refraction identity change, or the halo path
  feeding Refraction).
- Extra authority/resources (second program/draw/texture/FBO/scheduler, runtime change).
- Compile or layer regression (actual component shader compile/link failure, interaction or
  shadow regression).

## Stop

After one implementation/evidence round, return an Implementation Report and stop for GPT
Architecture Lead Implementation Architecture Review. Do not tune a second candidate, integrate
production, resize native bounds, add entry/exit/traversal lifecycle, commit, or archive.
