# Isolated Browser Lab Prototype Plan

Planning approval and explicit implementation authorization were received on 2026-08-23. The task is `in_progress`; execute only this checklist and stop for Architecture Review.

## 1. Freeze the Authority and Worktree

- Continue only in the isolated Paper Browser Lab worktree based on the accepted 0.0.80 baseline.
- Record the exact upstream core source, npm package versions/integrity, gitHead, retrieval date, and pre-change normalized source hash.
- Confirm the stable Thermal Production worktree is clean before and after the prototype.

## 2. Add the Single Derivative Seam

- Add one Lab-local upstream-derived fragment module with Apache/provenance/modified-file header.
- Preserve the official source byte-for-byte except unavoidable module routing and the approved `shape` to `1-shape` selector inversion.
- Reuse official exported preprocessing, sizing/default grammar, color parsing, and `ShaderMount`; do not copy or modify those systems.
- Add no generic patch/fork abstraction.

## 3. Mount One Isolated Comparison

- Reuse the accepted black r16 computational SVG, real panel, transparent `colorBack`, 200×200/r16 clip, and Paper defaults.
- Add one mutually exclusive Browser Lab control-versus-derivative mode.
- Suppress Production preview/readback/export controls exactly as existing Paper modes do.
- Do not pass timing, palette, morphology, coordinate, or calibration overrides.

## 4. Lock the Boundary in Tests

- Assert exact package pins and upstream commit/provenance.
- Assert the normalized source diff allowlist contains only the selector inversion and routing/header lines.
- Assert official preprocessing and `ShaderMount` imports remain package-owned.
- Assert no Production/Thermal/Refraction/carrier/filter/blend/mask/postprocess paths enter the new mode.
- Assert one panel, one Paper mount/canvas per candidate, Production exclusion, and build isolation.
- Assert Apache license, Paper NOTICE, and modified-file notice handling.

## 5. Run Falsifiable Browser Evidence

- Start the real Vite Browser Lab on an available temporary port and stop it afterward.
- Capture tight 200×200 element crops for control and derivative at 0/3/6/9/12 seconds; build only compact review sheets.
- Record independent native frame samples, mount/canvas counts, processed asset identity, disposal/remount, and Production-preview absence in JSON.
- Evaluate central interior occupancy/change separately from perimeter activity and visual desirability.
- Never claim exact phase synchronization between independent mounts.

## 6. Validation Commands

- Run the focused Paper/Lab source-fidelity and route-isolation test files.
- Run `npm run type-check`.
- Run `npm run lint -- --quiet`.
- Run `npm run build:renderer` and scan Production output for the derivative module/Paper identifiers.
- Run the full test suite, comparing any failure against the accepted clean baseline.
- Run `git diff --check` and a changed-path audit.

## 7. Report and Stop

- PASS only if the single selector inversion produces visibly changing Paper-native heat inside the panel while every source-fidelity/runtime/license gate passes.
- Otherwise record minimum-adaptation NO-GO and stop without a second formula, tuning pass, custom carrier, or Production proposal.
- Return the implementation report and compact evidence for Architecture Review; do not commit/integrate/archive unless separately authorized.
