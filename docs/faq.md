# FAQ

[中文](./faq.md) | [English](./faq.en.md)

## macOS 首次启动被系统拦截怎么办？

先尝试右键应用并选择“打开”，或前往 `系统设置 > 隐私与安全性` 放行。

如果仍然被 quarantine 阻止，可执行：

```bash
xattr -dr com.apple.quarantine "/Applications/FlowSelect.app"
```

## 下载失败或一直没有开始怎么办？

先确认：

- 输出目录仍然可写。
- 网络连接正常。
- FlowSelect 没有被系统防火墙或安全软件阻止。
- 当前站点仍在支持范围内，或当前链接本身仍然有效。

如果问题出现在网页视频下载流程，也可以尝试：

- 重新复制页面链接后再次粘贴。
- 重启应用后重试。
- 如果你使用浏览器扩展，确认扩展已连接到桌面端。

## 浏览器扩展显示未连接怎么办？

先确认：

- FlowSelect 桌面应用已经启动。
- 浏览器扩展弹窗中的状态不是 `Disconnected`。
- 本地通信没有被系统防火墙拦截。

如果问题持续存在：

- 关闭并重新打开桌面应用。
- 重新加载浏览器扩展。
- 重新打开扩展弹窗查看连接状态。

## 找不到下载后的文件怎么办？

- 默认输出目录为 `Desktop/FlowSelect_Received`。
- 双击主窗口空白区域，可以快速打开当前输出目录。
- 右键主窗口，可以打开当前输出目录或重新选择新的输出目录。

## 还需要更多帮助？

你可以先查看：

- [快速上手](./getting-started.md)
- [浏览器扩展](./browser-extension.md)

如果问题与某个版本有关，也可以去看对应版本的 [Release Notes](../release-notes/)。
